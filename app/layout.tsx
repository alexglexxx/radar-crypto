export default function RootLayout({children}:{children:React.ReactNode}){
  return <html lang="es"><body style={{background:'#0a0a0a',color:'white',margin:0,fontFamily:'system-ui'}}>{children}</body></html>
}
